# 数苹果大冒险 🍎

一个面向小学二年级学生的互动数学教育应用，通过趣味动画和游戏化方式教授"鸡兔同笼"类型的数学问题。

## 功能特点

- 🎮 **互动式学习**：通过动手操作理解数学概念
- 🦉 **AI 猫头鹰助手**：智能提示和引导（可选，需配置 Gemini API）
- 📚 **多种解题方法**：
  - 枚举法（基础）
  - 魔法侦探法
  - 填坑法
  - 青蛙跳跳法
  - 积木周期法
  - 差同减差法
  - 韩信点兵法
  - 层层堆叠法

## 环境要求

- **Node.js** 20.x 或更高版本
- **npm** 10.x 或更高版本

## 快速开始

### 1. 安装 Node.js

从 [Node.js 官网](https://nodejs.org/) 下载并安装 LTS 版本。

### 2. 安装依赖

```bash
cd 数苹果大冒险
npm install
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 访问应用

打开浏览器访问：**http://localhost:3000**

## 可选配置

### 启用 AI 猫头鹰助手

1. 获取 [Google Gemini API Key](https://aistudio.google.com/apikey)
2. 在项目根目录创建 `.env.local` 文件：

```
GEMINI_API_KEY=你的API密钥
```

3. 重启开发服务器

> 注意：如未配置 API Key，应用仍可正常运行，只是使用预设的提示文本。

## 项目结构

```
数苹果大冒险/
├── App.tsx              # 主应用组件
├── index.tsx            # 入口文件
├── index.html           # HTML 模板
├── index.css            # 全局样式
├── types.ts             # TypeScript 类型定义
├── vite.config.ts       # Vite 配置
├── components/          # UI 组件
│   ├── Apple.tsx        # 苹果图标组件
│   ├── OwlGuide.tsx     # 猫头鹰助手组件
│   └── PhaseCard.tsx    # 阶段卡片组件
└── services/
    └── geminiService.ts # Gemini AI 服务
```

## 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录。

## 预览生产版本

```bash
npm run preview
```

## 技术栈

- **React 19** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** (CDN) - 样式框架
- **Lucide React** - 图标库
- **Google Gemini AI** - AI 助手（可选）

## 许可证

MIT License
